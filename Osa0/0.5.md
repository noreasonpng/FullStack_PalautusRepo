```mermaid
sequenceDiagram
    participant browser
    participant server
    
    browser->>server: POST https://fullstack-exampleapp.herokuapp.com/new_note_spa
    activate server
    server-->>browser: HTTP 201
    server-->>browser: JSON-Content
    deactivate server

    Note right of browser: Unlike when visiting the regular site, after getting the status code after initial POST request, the site does not ask the browser
    to redirect and reload.
    
```
